{
  "name": "matatinta",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port ${PORT:-5173} --strictPort",
    "dev": "npm run start",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-4173}",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
